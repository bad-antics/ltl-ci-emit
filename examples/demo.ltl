// examples/demo.ltl — runnable demo for ltl_ci_emit

import std::io
use ltl_ci_emit as lib

fn main() {
    io::println("ltl_ci_emit demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}